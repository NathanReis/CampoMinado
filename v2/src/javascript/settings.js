export const SETTINGS = Object.freeze({
  campo: Object.freeze({
    linhas: 10,
    colunas: 10,
  }),
  bombas: Object.freeze({
    quantidade: 10,
    simbolo: '💣',
  }),
  bandeiras: Object.freeze({
    simbolo: '🚩',
  }),
});
